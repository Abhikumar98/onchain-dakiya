import { SearchIcon } from "@heroicons/react/outline";
import React from "react";

const SearchBar = () => {
	return (
		<div className="sticky -top-4 py-1 shadow-sm bg-secondaryBackground -mt-1"></div>
	);
};

export default SearchBar;
